apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- hardware-profile.yaml
# mig-config.yaml is intentionally NOT applied by this kustomization.
# It requires reviewed GPU Operator configuration and maintenance of the new A100 node.
