apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config.yaml
  - workbench.yaml
  - pipelines.yaml
  - rag.yaml
# RayJob is intentionally not continuously reconciled: submit it once per test drive.
  - playground.yaml
