apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../components/foundation
  - ../../components/storage
  - ../../components/mlflow
  - ../../components/evaluation
  - ../../components/mcp
  - ../../components/guardrails/mcp-integration
  - ../../components/science
  - ../../components/experience
  - ../../profiles/core
# GPU models, public MCP ingress and one-shot Ray jobs have explicit run gates.
# Secrets and shared operator patches are bootstrapped separately.
